{
  "name": "web3-metamask-bridge",
  "version": "1.0.0",
  "description": "Unity WebGL MetaMask bridge built with Vite (single-file IIFE for templates)",
  "license": "MIT",
  "author": "",
  "keywords": [
    "unity",
    "webgl",
    "metamask",
    "sdk",
    "bridge",
    "vite"
  ],
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "vite build",
    "package:zip": "npm run build && gzip -c dist/web3-metamask-bridge.js | base64 > dist/web3-metamask-bridge.js.gz.base64 && zip -j dist/bridge-payload.zip dist/web3-metamask-bridge.js.gz.base64",
    "preview": "vite preview --host",
    "test": "echo \"No tests for bridge\" && exit 0"
  },
  "engines": {
    "node": ">=19",
    "npm": ">=9"
  },
  "dependencies": {
    "@metamask/sdk": "^0.33.0"
  },
  "devDependencies": {
    "rimraf": "^6.0.0",
    "vite": "^7.1.3"
  }
}
